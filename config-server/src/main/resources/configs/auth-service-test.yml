spring:
  datasource:
    url: jdbc:mysql://localhost:3306/auth_db_test?useSSL=false&allowPublicKeyRetrieval=true&serverTimezone=UTC
    username: root
    password: root
  jpa:
    hibernate:
      ddl-auto: create-drop
    show-sql: false

logging:
  level:
    com.stationery.auth: WARN
